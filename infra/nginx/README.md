# Nginx Boundary

This directory is reserved for reverse-proxy configuration when deployment requires same-origin routing, TLS termination, or production traffic handling.

No Nginx configuration exists yet because the application services do not exist yet.

