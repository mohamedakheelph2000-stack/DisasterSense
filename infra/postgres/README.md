# PostgreSQL Boundary

This directory is reserved for database initialization or approved extension scripts. Database schema changes will be managed through backend Alembic migrations, not ad-hoc SQL files.

