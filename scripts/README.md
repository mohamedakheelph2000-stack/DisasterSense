# Shared Scripts

This directory will contain safe cross-project setup, verification, and release helper scripts after the relevant services exist.

Scripts must be documented, non-destructive by default, and safe to run from the repository root.

